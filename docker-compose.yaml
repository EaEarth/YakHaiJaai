version: "3.9"
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    container_name: app_frontend
    networks:
      - app
    depends_on:
      - backend
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/app
    networks:
      - app
    container_name: app_backend
    depends_on:
      - mysql
  mysql:
    image: mysql:8
    volumes:
      - mysql:/var/lib/mysql
    environment:
      - MYSQL_DATABASE=userdb
      - MYSQL_USER=app_user
      - MYSQL_PASSWORD=userpass
      - MYSQL_ROOT_PASSWORD=Ea14148204232
    container_name: app_db
    ports:
      - "8080:8080"
    networks:
      - app
volumes:
  mysql:
networks:
  app:
    driver: bridge
